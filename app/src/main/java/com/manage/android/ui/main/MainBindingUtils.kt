package com.manage.android.ui.main

import androidx.databinding.BindingAdapter
import com.google.android.material.tabs.TabLayout
import com.manage.android.data.local.db.dao.Category

/*
@BindingAdapter("app:setTabs")
fun setTabLayoutItems(tabLayout: TabLayout, list: List<Category>?) {
    tabLayout.removeAllTabs()
    if(list != null) {
        for(i in list) {
            tabLayout.addTab(tabLayout.newTab().setText(i.categoryName))
        }
    }
}*/
